%h2= t(:new_reply)

- if @reply.errors.any?
  = pluralize(@reply.errors.count, "error")

  %ul
    - @reply.errors.full_messages.each do |msg|
      %li= msg


= form_for([@article, @reply]) do |f|
  .field
    = f.label "Author: "
    = f.text_field :author

  .field 
    = f.label "Reply: "
    = f.text_area :content

  .action
    = f.submit "Post"