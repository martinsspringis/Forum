%h2= t(:new_reply)

- if @reply.errors.any?
  = pluralize(@reply.errors.count, "error")

  %ul
    - @reply.errors.full_messages.each do |msg|
      %li= msg


= form_for([@article, @article.replies.build]) do |f|
  .field
    = f.label "Author: "
    = f.text_field :author

  .field 
    = f.label "Reply: "
    = f.text_area :body

  .action
    = f.submit "Post"