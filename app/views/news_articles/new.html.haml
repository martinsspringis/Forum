= render "shared/main_menu"

.container
  %h1= t(:new_article)
  %br
  = simple_form_for @article do |f|
    = f.input :title, label: t(:title)
    = f.input :article, label: t(:article)
    = f.input :image, label: t(:choose_image)
    = f.button :submit, t(:save_article)